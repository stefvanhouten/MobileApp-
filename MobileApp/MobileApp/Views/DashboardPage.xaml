<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Views.DashboardPage"
             xmlns:vm="clr-namespace:MobileApp.ViewModels"
             Title="{Binding Title}">
    <!--Connect viewmodel to this page-->
    <!--<ContentPage.BindingContext>
        <vm:DashboardViewModel />
    </ContentPage.BindingContext>-->
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to the dashboard!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label x:Name="StatusLabel" Text="{Binding Test}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <ListView x:Name="MQResultList" ItemsSource="{Binding MQMessage}" SelectionMode="None" HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding .}"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Connect" Command="{Binding ConnectCommand}"></Button>
            <Button Text="Publish" Command="{Binding PublishCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>